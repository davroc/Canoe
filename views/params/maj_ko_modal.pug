extends ../layouts/layout.pug

block styles
    link(rel="stylesheet", href="/css/forms.css")

    .modal.fade(id="KOModifParamModal",tabindex="-1", role="dialog")
      .modal-dialog(role="document")
        .modal-content
          .modal-header.KO-modal
            i.material-icons done_outline
            h5.modal-title Erreur lors de la mise a jour!!
              input(type="hidden", id ="redir" ,value = returnPath)
          .modal-body.KO-modal
            span.success #{Message}
            
          .modal-footer
            input.btn.btn-modal-KO(type="button",id="btn_confirm",value="OK")
    script.
      $('#KOModifParamModal').modal('show')
      $('#btn_confirm').click(function(){
        $(location).attr('href',$('#redir').val());
      })