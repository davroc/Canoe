extends ../layouts/layout.pug

block styles
    link(rel="stylesheet", href="/css/forms.css")

block content
    main
        form.product-form(action="./login", method="POST")
            .form-control
                label(for="login_user") Login
                input(type="text", name="login_user",value="")
                label(for="pwd_user") Mot de Passe
                input(type="password", name="pwd_user",value="")
            button.btn(type="submit") Se connecter